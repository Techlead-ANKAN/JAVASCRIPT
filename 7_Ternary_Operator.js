// Ternary Operator - Evaluates a condition and executes a code based on the condition

// Syntax: -
// condition ? exp1 : exp2

let marks = 38;
res = marks > 35 ? "Pass" : "Fail"; // if condition met then it will show pass or else fail

console.log(res);
